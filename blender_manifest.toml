schema_version = "1.0.0"

id = "boardtyper_3d"
version = "1.0.0"
name = "Boardtyper 3D"
tagline = "Export Blender models to Tabletop Simulator JSON"
maintainer = "Skarmuse"
type = "add-on"

blender_version_min = "4.2.0"

license = [
  "SPDX:GPL-3.0-or-later",
]

tags = ["Import-Export", "Game Engine"]

[urls]
website = "https://linktr.ee/skarmuse"

[permissions]
files = "Export OBJ and JSON files to the local drive"